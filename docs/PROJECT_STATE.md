# Project State

## Project

SevenBet

## Current Phase

Phase 1 — Product Master Plan, supported by Product & Design preparation

## Completed

✅ Product Vision

✅ Technical Baseline verified against the actual implementation repository

✅ Product & Design visual reset — RFC-007 approves “Tilt-Locked Human Product Theatre” and supersedes Human Guidance

✅ Legacy Figma foundation baseline recorded: 7 collections, 77 variables, semantic Light / Ink / Help collections, 10 Manrope text styles, and 3 elevation styles

## In Progress

🟨 Product Master Plan

🟨 RFC-002 — Active Control Program and Personal Control Dashboard approved; Mission 01 → registration → Dashboard → Missions 02–04 is implemented, while Missions 05–10 and broader product/compliance gates remain

🟨 RFC-004 — Commercial Launch Delivery Plan approved; Milestone 0 launch contract is the next implementation gate

✅ RFC-007 — Tilt-Locked Human Product Theatre approved; Figma foundations/components and interactive Next.js Home validated on desktop

✅ Product & Design reset — superseded Prismatic, Human Guidance and earlier concept foundations, components, screens, variables and styles removed from the active Figma file

✅ Tilt-Locked public acquisition surfaces — Home, Bonuses, Best Offers, Casinos, the casino profile/review pattern and a representative protected-control article detail complete in Figma; the article-detail system is governed by the RFC-007 protected-control / educational-commercial amendment; mobile/device QA and broader journey migration remain pending

✅ Tilt-Locked responsive public shell — Figma family `492:2268` completes the responsive Header (`289:43`), Footer (`488:100`) and generic Availability Notice (`489:70`) across 1,440 desktop, 1,280 laptop, 390 × 844 mobile and 375 × 667 small-mobile representatives; signed-out/in, menu open/closed, unknown/unavailable, disclosure and protected-Help handoff states are internally QA-validated, and the desktop/laptop/mobile Public Shell is visually approved. It is the shared visual and navigation source of truth for subsequent public P0 screen families; live market authority and frontend parity remain pending

✅ Tilt-Locked `/10-steps` screen family — desktop family `502:2238` and mobile family `502:2412` cover full signed-out 1,440, 1,280 and 390 layouts plus 390×844 returning signed-in and 375×667 signed-out first folds. The family reuses the approved Public Shell, passes internal visual/structural QA and is visually approved; frontend parity is not implemented

✅ Tilt-Locked active-control package — Mission 01, mandatory earned-result registration, the authenticated Dashboard, Mission 02, truthful XP/achievement/active-day states, and the post-Mission-02 Dashboard complete and visually validated in Figma

✅ Active-control backend slice — RFC-008, additive Prisma migrations through 0015, private anonymous session/claim, atomic Mission 01 and Mission 02 persistence, Dashboard API, artefact controls, deterministic rewards, active-day correction and targeted tests implemented and verified against the configured remote database

✅ Active-control frontend slice — `/program` now implements the approved Mission 01 → earned-result registration → Dashboard → Mission 02 → updated Dashboard journey against the RFC-008 API, including responsive Tilt-Locked screens, evidence notes, private artefact editing and deterministic reward states

✅ Mission 03 active-control slice — RFC-009 approved; the eight-screen evidence-led mission, private `UrgeLearningRecord`, resumable autosave, learning checks, `not now` branch, editable result, deterministic `+90 XP`, Dashboard `230 XP / 3 of 10` and Mission 04 current state are implemented and browser/runtime-verified

✅ Mission 04 active-control slice — RFC-010 approved; the nine-stage build-and-rehearse mission, live Boundary Composer, private editable `ActiveBoundary`, deterministic `+100 XP`, `Boundary built`, Dashboard `330 XP / 4 of 10` and Mission 05 current state are implemented; targeted tests, type checking and the connected migration are verified, while authenticated browser completion and release review remain gates

✅ Active-control backend architecture hardening — Missions 01–04 are split into bounded application/domain/infrastructure/validation slices with an explicit transaction-aware unit of work, server-owned repeatable Dashboard snapshots and permanent Programme engineering standards; public APIs, schema, rewards, product behaviour and frontend are unchanged

## Progress

Technical Baseline completed.

Documentation structure normalized before Phase 2.

Product & Design preparation is aligned with the approved Product Vision: the 10-Step Control Program is the central product layer; discovery remains accessible without programme completion; and Help / self-exclusion is a protected, non-commercial route. RFC-007 supersedes RFC-006 and approves “Tilt-Locked Human Product Theatre” as the active visual direction.

Figma Plugin API access was revalidated after the file was upgraded to a Professional plan. RFC-007 is the active visual authority. Superseded Prismatic, Human Guidance and earlier concept artefacts have been removed from the active Figma file. The retained design system consists of the Tilt-Locked foundations, reusable Core components and SevenBet-specific product components. The desktop screen page now contains the accepted Home (`289:946`), Bonuses (`299:786`), Best Offers (`310:224`), Casinos (`325:323`), illustrative casino profile/review (`343:601`) and representative protected-control article detail (`372:648`) frames. The article system adds documented article components (`368:38`) and a reusable Related Reading Row (`369:38`). Its representative 1,440 × 6,507 px article uses continuous editorial reading, one human-agency image, a direct answer, four numbered action chapters, a single Program action, official-source layer, protected related reading, FAQ, protected Help and public footer; casino, bonus, best-offer and affiliate actions are excluded from the article body. Casinos includes backend-compatible Review Signal (`320:5`), Filter Chip (`321:6`), Filter Option (`332:548`), Review Card (`322:2`), Featured Review Theatre (`324:8`) and Key Fact Row (`342:38`) components. Its catalogue uses a full-width filter command centre (`333:535`) with search, active-filter removal, all six multi-select backend facets, four availability filters, counts, sort, page size, reset/apply actions and preserved three-card comparison width. It also explains licence/market, payments, controls, Editor Score and commercial eligibility. The casino profile is now a 1,440 × 4,466 px commercial decision profile rather than the superseded 9,028 px dossier. It presents the offer, Editor Score, verdict, material restrictions and disclosed outbound action in the first viewport, followed by a sticky decision bar, concise pros and constraints, reusable key facts, a single tab-ready details panel, a short score rationale, four FAQ prompts, repeated final offer action, protected Help and the public footer. Northstar remains illustrative and must not ship without published backend data and market, editorial, compliance and commercial review. Because no casino snapshot is currently published, the production empty state remains explicit. Best Offers includes a three-state terms-first shortlist slider (`308:81`), ranking methodology, evidence fields, editorial reasoning, FAQ, related discovery paths, protected Help and affiliate/legal disclosure. Illustrative commercial content is explicitly marked for verification before release. The same Home is implemented at `/` in Next.js with an interactive programme carousel, persistent navigation, responsive layout, motion/reduced-motion behaviour, programme-tool states and working Program/Help routes. Mobile/device QA and broader journey migration remain pending.

**Detected and approved, 2026-08-04:** the active Figma mobile page contains the responsive Public Shell family (`492:2268`). The Header set (`289:43`) has eight desktop/mobile, theme, account and menu variants; the Footer set (`488:100`) has desktop/mobile variants; and the generic Availability Notice set (`489:70`) has desktop/mobile unknown/unavailable variants. Representative screens cover 1,440 (`492:2283`), 1,280 (`494:116`), three 390 × 844 states (`493:57`, `493:78`, `493:115`) and 375 × 667 (`493:131`). Internal QA detected approved fonts only, no detached instances, no placeholder or text-bound defects, minimum 44 px audited targets, and seven Menu/Close/Escape prototype-reaction nodes. The desktop, laptop and mobile Public Shell states are visually approved and govern subsequent public P0 screen families as the shared shell; this approval does not claim live jurisdiction detection or frontend parity.

**Detected and approved, 2026-08-05:** `/10-steps` is designed in Figma as desktop family `502:2238` and mobile family `502:2412`, with approved full signed-out 1,440 (`502:2240`), 1,280 (`502:2241`) and 390 (`502:2414`) landings, an approved 390 × 844 returning signed-in first fold (`502:2415`) and an approved 375 × 667 signed-out first fold (`502:2416`). The family reuses the approved Public Header and Footer, keeps Mission 01 as the only body conversion, keeps commercial discovery in the shared shell, and makes account creation a post-Mission-01 boundary. Internal QA found 67 linked instances, no detached instances, no horizontal overflow, no placeholder text and no audited targets below 44 px. The user visually approved the responsive family; its node IDs are unchanged. The current frontend remains legacy/stale and still displays `+20 XP`; no application code was changed.

The 2026-08-03 RFC-006 amendment makes `Start the 10-Step Program` the only conversion action inside Home content. Commercial discovery remains directly available through the global header and footer, while the Home body uses static self-recognition, evidence limitations, a visible Personal Control Plan result, non-testimonial plan-use scenarios, methodology and a protected Help route. Program, Help, pause, self-check and vulnerability data are prohibited from advertising targeting and individual commercial personalisation. The 2026-08-04 RFC-007 article amendment applies the same separation to Learning Center content: neutral commercial education may link contextually to discovery after delivering value, while quit, control, self-exclusion and harm-related articles use a protected-control template with no casino, bonus, offer or affiliate actions in article content.

RFC-002 and its 2026-08-04 active-work amendment remain the approved product authority for the Personal Control Dashboard, mandatory account entry after the first useful Programme mission, 15–25 minute active-work missions, and deterministic Programme rewards. The active Figma file contains the reusable Programme component board (`387:38`), the Mission 01 → Registration → Dashboard → Mission 02 flow (`426:2`), Mission 03 plus its post-mission Dashboard (`449:1413`), and the approved Mission 04 flow plus Dashboard (`468:1753`). RFC-008 governs shared persistence, rewards and privacy; RFC-009 governs Mission 03; RFC-010 governs Mission 04. Mission 01 awards `+60 XP`; Mission 02 awards `+80 XP` and `First Plan`; Mission 03 awards `+90 XP`; Mission 04 awards `+100 XP` and `Boundary built`; same-day completion remains one active day. **Detected:** `/program` implements Missions 01–04, mandatory account claim, returning-user sign-in and their approved Dashboard states. **Detected:** migrations 0011–0017 are applied to the configured remote database. **Detected:** the hardened Programme suite has 43 passing tests covering deterministic domain policy, concurrency/replay, rollback, ownership, Dashboard truthfulness, active-day correction and public error mapping; the full Node suite has 209 passing tests, and typecheck, Prisma schema validation and the production build pass. **Detected limitation:** repository lint remains unconfigured for Next 15 because `npm run lint` invokes interactive/deprecated `next lint`. **Planned release gates:** authenticated Mission 04 browser completion, clinical-content/compliance review, broader device QA, expiry purge automation, distributed rate limiting, account-wide export/erasure, telemetry and Missions 05–10.

## Technical Baseline Audit

| Field | Value |
| --- | --- |
| Audit date | 2026-07-28 |
| Repository root | `/Users/alex/Documents/Codex/2026-07-09/ns/sevenbet-next` |
| Technical Baseline path | `docs/05_Engineering/Technical_Baseline/` |
| Implementation maturity | Substantial pre-architecture implementation: Next.js public/admin application, Prisma/PostgreSQL, Better Auth, CMS builders, casino/affiliate/media/progress modules, and focused tests are detected. Production-operational and compliance evidence remains incomplete. |
| Architecture readiness score | 5/10 |
| Main blockers before Phase 2 | Authoritative architecture alignment; jurisdiction/licensing and safety-control design; CI/CD, deployment/migration, observability and operational controls; reconciliation of implementation with the approved Product Vision. |

The formal Phase 2 architecture milestone remains pending. A bounded, evidence-backed Programme backend hardening is implemented for Missions 01–04; it does not claim completion of the wider target architecture.

## Pending

⬜ Product Architecture

⬜ Domain Model

⬜ Compliance

🟨 Engineering Standards — Programme-specific architecture, backend and Definition-of-Done standards are implemented; phase-wide standards remain pending

⬜ RFC

## Current Milestone

Complete Product Master Plan while extending the validated Tilt-Locked consumer-product system across public acquisition and active-control surfaces

## Next Milestone

Complete authenticated browser, mobile/device, clinical-content and compliance release review for Mission 04, then design and approve Mission 05 while continuing the remaining operational privacy controls
