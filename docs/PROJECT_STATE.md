# Project State

## Project

SevenBet

## Current Phase

Phase 1 — Product Master Plan, supported by Product & Design preparation

## Completed

✅ Product Vision

✅ Technical Baseline verified against the actual implementation repository

✅ Product & Design visual reset — RFC-007 approves “Tilt-Locked Human Product Theatre” and supersedes Human Guidance

✅ Legacy Figma foundation baseline recorded: 7 collections, 77 variables, semantic Light / Ink / Help collections, 10 Manrope text styles, and 3 elevation styles

## In Progress

🟨 Product Master Plan

✅ Documentation-only Frontend Migration Audit — all 59 approved Figma screen families are mapped to current Next.js evidence and P0 work packages in `docs/02_Product_Design/Frontend-Migration-Audit-and-P0-Implementation-Plan.md`. The audit records 0 `PARITY`, 11 `CLOSE_PARITY`, 14 `PARTIAL`, 21 `STALE`, 3 `PLACEHOLDER`, 3 `FRONTEND_MISSING`, 3 `BLOCKED_BY_DATA` and 4 `BLOCKED_BY_PRODUCT`. `/self-check` and `/tools/budget-calculator` remain P0 as `P0_REDESIGN_REQUIRED`; their current mechanics are not approved, and production release is blocked by FE-SAFETY-01 plus separate product/privacy/compliance/Figma approval. No application, backend, API, Prisma or Figma change was made.

✅ FE-MIG-01 Public Shell — [PR #15](https://github.com/AlexG-7BE/sevenbet-next/pull/15) is merged in `ceee4e7`. The approved Figma Header `289:43`, Footer `488:100` and non-authoritative Availability Notice `489:70` are represented through a server-owned `(public)` layout, Better Auth account state and a bounded client mobile menu. Programme, Protected Help and admin/internal shells remain separate.

✅ FE-MIG-02 Home responsive parity — [PR #16](https://github.com/AlexG-7BE/sevenbet-next/pull/16) is merged in `bef86bd`. The unchanged approved desktop/mobile Home family is represented through a server-rendered ten-section body with one bounded carousel client island. Critical content no longer depends on reveal hydration, responsive/no-JS/accessibility regressions pass, and shared server-owned account presentation remains authoritative. No backend/API/Prisma, Programme policy/data, protected Help, commercial eligibility, safety-tool mechanic or Figma change is included.

🟨 FE-MIG-03 10 Steps campaign landing — implemented on `codex/fe-mig-03-ten-steps` in [PR #17](https://github.com/AlexG-7BE/sevenbet-next/pull/17), approved subject to the completed bounded correctness fix and pending squash merge. The approved desktop/mobile body replaces legacy `+20 XP`, unsupported market claims and local commercial actions. Anonymous `+60 XP` is pending-only with `Awarded when Mission 01 is saved to your account.` Returning XP/progress come from the server Dashboard projection; the existing registry completion capability keeps Missions 01–04 resumable and maps post-Mission-04 current Mission 05 to `available-programme-complete` without a Mission 05 link or next-step claim. Missions 05–10 remain visibly planned/not available, and responsive/no-JS/accessibility regressions pass. No backend/API/Prisma, Programme policy/data, protected Help, commercial eligibility or Figma change is included.

🟨 RFC-002 — Active Control Program and Personal Control Dashboard approved; Mission 01 → registration → Dashboard → Missions 02–04 is implemented, while Missions 05–10 and broader product/compliance gates remain

🟨 RFC-004 — Commercial Launch Delivery Plan approved; Milestone 0 launch contract is the next implementation gate

✅ RFC-007 — Tilt-Locked Human Product Theatre approved; Figma foundations/components and interactive Next.js Home validated on desktop

✅ Product & Design reset — superseded Prismatic, Human Guidance and earlier concept foundations, components, screens, variables and styles removed from the active Figma file

✅ Tilt-Locked public acquisition surfaces — Home, Bonuses, Best Offers, Casinos, the casino profile/review pattern and a representative protected-control article detail complete in Figma; the article-detail system is governed by the RFC-007 protected-control / educational-commercial amendment; mobile/device QA and broader journey migration remain pending

✅ Tilt-Locked responsive public shell — Figma family `492:2268` is merged through frontend PR #15 with the approved responsive Header (`289:43`) and revised Footer (`488:100`) across the validated 1,440, 1,280, 390 × 844 and 375 × 667 representatives. The Footer keeps compact `Open Help`; generic Availability Notice (`489:70`) remains presentation-only with no live market authority. Authenticated browser evidence and legal/compliance wording approval remain pending

✅ Tilt-Locked Comparison family — desktop family `567:3592` and mobile family `569:1589` are visually approved across full 1,440/390 layouts, 1,280/375 first folds and ten desktop/mobile selection, compatibility, eligibility, recovery and outbound states. Labels `567:3593` and `569:1590` record `APPROVED`; all family/frame node IDs are unchanged. The family reuses the approved Public Shell; no `/compare` frontend route or comparison engine is detected

✅ Tilt-Locked Programme responsive family — mobile family `580:1713` is visually approved across 17 Mission 01–04/registration/Dashboard journey representatives, 13 recovery and artefact-control states, and four 375 first folds. Label `580:1714` records `APPROVED`; existing desktop Programme families and component sources are preserved and no frontend code was changed

✅ Tilt-Locked Protected Help responsive family — desktop family `599:3886` and mobile family `600:1713` are visually approved across the dedicated protected shell, full Hub and self-exclusion article, 1,280/375 first folds and verified, unavailable, content-blocked, outbound and article-recovery states. Labels `599:3887` and `600:1714` record `APPROVED`; all node IDs are unchanged and frontend code was not changed

✅ Tilt-Locked Identity & Privacy responsive family — desktop family `613:4023` and mobile family `624:1930` are visually approved across returning sign-in, invalid credentials, preserved-claim recovery, public/authenticated privacy controls, a truthful password-recovery-unavailable state and 1,280/375 first folds. Labels `613:4024` and `624:1931` record `APPROVED`; all node IDs are unchanged and frontend code was not changed

✅ Tilt-Locked Learning & Articles responsive family — desktop family `632:4237` and mobile family `634:2074` are visually approved across catalogue, category, neutral article, evidence/source, article-unavailable and 1,280/375 first-fold states. Labels `632:4238` and `634:2075` record `APPROVED`; the approved Public Shell is reused, approved Protected Help article nodes `599:3972` / `600:1792` are unchanged, and frontend code was not changed

✅ Tilt-Locked Legal / Trust / System responsive family — desktop family `646:4467` and mobile family `649:2257` are visually approved across Affiliate Disclosure, Ranking Methodology, About/18+, governed Privacy/Terms/Responsible Gambling boundaries, public/protected-safe recovery, availability and loading representatives, plus 1,280/375 contracts. Labels `646:4468` and `649:2258` record `APPROVED`; all family/frame/component node IDs are unchanged and frontend code was not changed

✅ Tilt-Locked Home responsive family — preserved full desktop Home `289:946`, desktop family `661:7551` and mobile family `657:2545` are visually approved across the 1,280 returning-user first fold, full 390 narrative, approved-shell menu-open state, 375 first fold and responsive/accessibility contracts. Labels `661:7552` and `657:2546` record `APPROVED`; all family/frame node IDs are unchanged and frontend code was not changed

✅ Tilt-Locked Programme Map / My Plan responsive family — desktop family `666:4862` and mobile family `668:2671` are visually approved across full ten-node after-M01/after-M04 maps, 1,280/375 first folds, Mission 04 pause/re-entry, disabled Mission 05 next-package and Dashboard loading/retry states. Labels `666:4863` and `668:2672` record `APPROVED`; all family/frame node IDs are unchanged and frontend code was not changed

✅ Tilt-Locked Pause / Support responsive family — desktop family `674:5143` and mobile family `674:8171` are visually approved across a protected cooling-off article, 1,280/375 first folds, terms-unavailable, local-guidance-blocked, article-unavailable and protected-handoff states. Labels `674:5144` and `674:8172` record `APPROVED`; the approved Protected Help shell and source components are unchanged, content remains review-gated, and frontend code was not changed

✅ Tilt-Locked Commercial Handoff / Affiliate Outbound responsive family — desktop family `679:5238` and mobile family `679:8391` are visually approved across disclosure-before-action, outbound-confirmation, action-unavailable, redirect-failure and 1,280/375 contracts. Labels `679:5239` and `679:8392` record `APPROVED`; the approved Public Shell is unchanged and frontend code was not changed

✅ Tilt-Locked Age / Market Boundary responsive family — desktop family `686:5333` and mobile family `686:8301` are visually approved across generic age-policy-unresolved, availability-unknown, signal-conflict/stale, restricted/unsupported, policy-unavailable/suspended and 1,280/375 contracts. Labels `686:5334` and `686:8302` record `APPROVED`; live age/market authority remains blocked and frontend code was not changed

✅ Tilt-Locked Bonus Guide / Explainer responsive family — desktop family `694:5455` and mobile family `694:8724` are visually approved across full 1,440/390 editorial articles, 1,280/375 first folds, verified/review-due/unavailable evidence and article-under-review recovery. Labels `694:5456` and `694:8725` record `APPROVED`; editorial/source approval remains separate and frontend code was not changed

✅ Tilt-Locked `/10-steps` screen family — desktop family `502:2238` and mobile family `502:2412` cover approved full signed-out 1,440, 1,280 and 390 layouts plus approved 390×844 returning signed-in and 375×667 signed-out first folds. The family reuses the approved Public Shell, removes the standalone Help panel, and passes revised visual/structural QA. FE-MIG-03 implements the family on `codex/fe-mig-03-ten-steps`; authenticated browser evidence remains a release gap pending an approved disposable fixture

✅ Tilt-Locked `/casinos` responsive family — desktop family `520:2496` and mobile family `521:312` are approved across full 1,440/390 layouts, 1,280/375 first-fold contracts, filter-open/applied, loading, empty, no-eligible, unknown/unavailable and error states. Original desktop anchor `325:323` and the approved Public Shell are preserved; live market authority and frontend parity remain pending

✅ Tilt-Locked active-control package — Mission 01, mandatory earned-result registration, the authenticated Dashboard, Mission 02, truthful XP/achievement/active-day states, and the post-Mission-02 Dashboard complete and visually validated in Figma

✅ Active-control backend slice — RFC-008, additive Prisma migrations through 0015, private anonymous session/claim, atomic Mission 01 and Mission 02 persistence, Dashboard API, artefact controls, deterministic rewards, active-day correction and targeted tests implemented and verified against the configured remote database

✅ Active-control frontend slice — `/program` now implements the approved Mission 01 → earned-result registration → Dashboard → Mission 02 → updated Dashboard journey against the RFC-008 API, including responsive Tilt-Locked screens, evidence notes, private artefact editing and deterministic reward states

✅ Mission 03 active-control slice — RFC-009 approved; the eight-screen evidence-led mission, private `UrgeLearningRecord`, resumable autosave, learning checks, `not now` branch, editable result, deterministic `+90 XP`, Dashboard `230 XP / 3 of 10` and Mission 04 current state are implemented and browser/runtime-verified

✅ Mission 04 active-control slice — RFC-010 approved; the nine-stage build-and-rehearse mission, live Boundary Composer, private editable `ActiveBoundary`, deterministic `+100 XP`, `Boundary built`, Dashboard `330 XP / 4 of 10` and Mission 05 current state are implemented; targeted tests, type checking and the connected migration are verified, while authenticated browser completion and release review remain gates

✅ Active-control backend architecture hardening — Missions 01–04 are split into bounded application/domain/infrastructure/validation slices with an explicit transaction-aware unit of work, server-owned repeatable Dashboard snapshots and permanent Programme engineering standards; public APIs, schema, rewards, product behaviour and frontend are unchanged

## Progress

Technical Baseline completed.

Documentation structure normalized before Phase 2.

Product & Design preparation is aligned with the approved Product Vision: the 10-Step Control Program is the central product layer; discovery remains accessible without programme completion; and Help / self-exclusion is a protected, non-commercial route. RFC-007 supersedes RFC-006 and approves “Tilt-Locked Human Product Theatre” as the active visual direction.

Figma Plugin API access was revalidated after the file was upgraded to a Professional plan. RFC-007 is the active visual authority. The retained design system consists of Tilt-Locked foundations, reusable Core components and SevenBet-specific product components. The desktop screen page contains Home (`289:946`), Bonuses (`299:786`), Best Offers (`310:224`), Casinos (`325:323`), illustrative casino profile/review (`343:601`) and representative protected-control article detail (`372:648`). On 2026-08-05 the standalone Protected Help panel was removed from every active public desktop screen; Help remains available in the persistent Header and through the revised Footer `CONTROL & SUPPORT` link. The revised public heights are Home 7,680px, Bonuses 7,900px, Best Offers 9,430px, Casinos 9,258px, Casino Profile 4,262px and protected-control article 6,255px, with no direct-child overflow. The representative article still excludes casino, bonus, best-offer and affiliate actions from the body. Northstar and all illustrative commercial content remain blocked from production without published data and market, editorial, compliance and commercial review. Merged frontend PR #15 owns the shared Home shell; PR #16 implements approved responsive Home body parity without changing Programme or commercial authority.

**Detected and merged in PR #15, revised 2026-08-05:** the active Figma mobile page contains the responsive Public Shell family (`492:2268`). The Header set (`289:43`) has eight desktop/mobile, theme, account and menu variants; the Footer set (`488:100`) has approved desktop/mobile variants; and the generic Availability Notice set (`489:70`) has desktop/mobile unknown/unavailable variants. Representative approved screens cover 1,440 (`492:2283`), 1,280 (`494:116`), three 390 × 844 states (`493:57`, `493:78`, `493:115`) and 375 × 667 (`493:131`). Frontend PR #15 implements shared route-group ownership, server-aware account presentation, the responsive modal menu, the Footer `CONTROL & SUPPORT / Open Help` entry and one-main ownership. It does not claim live jurisdiction detection; authenticated browser evidence remains pending.

**Detected, approved and implemented on `codex/fe-mig-03-ten-steps`, revised 2026-08-05:** `/10-steps` is designed in Figma as desktop family `502:2238` and mobile family `502:2412`, with approved full signed-out 1,440 (`502:2240`), 1,280 (`502:2241`) and 390 (`502:2414`) landings, a 390 × 844 returning signed-in first fold (`502:2415`) and a 375 × 667 signed-out first fold (`502:2416`). FE-MIG-03 replaces the stale body with the seven approved body sections and the shared Footer, removes `+20 XP`, `UK PREVIEW`, `UK-ready discovery` and local commercial actions, and keeps `+60 XP` pending with the exact copy `Awarded when Mission 01 is saved to your account.` Returning XP and completed count are read from the server Dashboard projection; unavailable projection fails closed without invented values. The resolver uses the existing Mission registry `completion !== null` capability: current Missions 01–04 remain resumable, while post-Mission-04 current Mission 05 maps to the separate `available-programme-complete` state, retains server totals, offers only `Open My Programme` and never presents Mission 05 as available or next. Missions 05–10 are labelled planned/not yet available. Contract, no-JS, reduced-motion, responsive and cross-route browser regressions pass from 1,440 to 320 CSS pixels. Frontend PR #15 remains the only shared-shell/main owner. No backend/API/Prisma, Programme rule, protected Help, commercial eligibility or Figma node was changed. An authenticated browser fixture/screenshot remains not detected and is recorded as a review gate.

**Detected and approved, 2026-08-05:** `/casinos` is designed as desktop family `520:2496` and mobile family `521:312`. Coverage includes canonical full 1,440 (`520:2498`), 1,280 first fold (`520:2741`), desktop states (`520:2742`), full 390 (`521:314`), filter-open (`521:315`), filter-applied (`521:316`), loading (`521:317`), no matches (`521:318`), mobile availability/error matrix (`521:319`) and 375 first fold (`521:320`). Family labels `520:2497` and `521:313` record approval without changing any family/frame node ID. The original desktop anchor `325:323` was not altered. The family reuses linked approved Public Header/Footer instances and existing Casino components, has no direct-child overflow on the canonical full frames, and meets the 44px minimum for audited interactive mobile filter targets. Generic restricted/unknown treatments are illustrative only: trusted jurisdiction authority is not detected. The current frontend supports the discovery query surface but remains visually legacy/partial; no application code was changed.

**Detected and approved, 2026-08-05:** `/casino/[slug]` is designed as desktop family `529:2850` and mobile family `530:809`, retaining the original Northstar desktop anchor `343:601` unchanged. Coverage includes canonical full 1,440 (`529:2852`), 1,280 first fold (`529:3075`), desktop fail-closed contract (`529:3299`), full 390 (`530:811`), outbound confirmation (`531:3983`), visit unavailable with the editorial review retained (`531:3984`), unpublished/archived profile unavailable (`531:3985`), redirect failure (`531:3986`) and 375 first fold (`531:3987`). Family labels `529:2851` and `530:810` record approval without changing any family/frame node ID. The family reuses the approved Public Shell, keeps commercial action eligibility separate from editorial availability, exposes no raw destination URL, and preserves protected Help. Canonical full frames and fixed-height state frames have no direct-child overflow; only first-fold and dimmed-background crops are deliberate. Trusted live jurisdiction authority remains not detected, all Northstar content remains illustrative, and no application code was changed.

**Detected and approved, revised 2026-08-05:** `/bonuses` is designed as desktop family `541:3002` and mobile family `541:3950`, retaining the original desktop anchor `299:786` unchanged. Coverage includes canonical full 1,440 (`541:3952`), 1,280 first fold (`541:4216`), desktop state contract (`542:3328`), full 390 (`542:4329`), filter-open (`544:4415`), filter-applied (`544:4416`), loading (`544:4417`), offer availability (`544:4418`), market/error states (`544:4419`), outbound confirmation (`544:4420`), redirect failure (`544:4421`) and 375 first fold (`544:4422`). Family labels `541:3951` and `542:4328` record `APPROVED`. The user visually approved the family after display/serif overlaps were removed from the 390 source, outbound-background copy and 375 crop. The family uses linked approved Public Header/Footer sources, preserves material terms before action, removes operator CTA from expired/ineligible states, exposes no raw destination URL and keeps protected Help available. Audited full/fixed frames have no unexpected direct-child overflow and audited actions meet the 44px minimum. Live market/offer authority remains not detected, all offer content remains illustrative, the current frontend is legacy/partial, and no application code was changed.

**Detected and approved, 2026-08-05:** `/best-offers` is designed as desktop family `556:3336` and mobile family `557:1470`, retaining the original desktop anchor `310:224` unchanged. Coverage includes canonical full 1,440 (`556:3338`), 1,280 first fold (`556:3470`), desktop nine-state contract (`556:5324`), full 390 (`557:1472`), 375 first fold (`558:1507`) and mobile nine-state contract (`558:1616`), including eligible shortlist, methodology, loading, no eligible offers, unknown/restricted availability, listings unavailable, load error, affiliate outbound confirmation (`560:5174`) and redirect failure (`558:1723`). Family labels `556:3337` and `557:1471` record `APPROVED`; all family/frame node IDs are unchanged. The user visually approved the family on 2026-08-05. The family reuses the approved Public Shell, places material terms and why-shortlisted reasoning before action, defines a published non-commission-only ranking method, and exposes no operator action while eligibility is unresolved. Audited mobile/full state compositions have no unexpected text overflow; Header/Footer links remain intact. Live market/ranking authority and a dedicated canonical Best Offers projection are not detected, all operator content remains illustrative, the current frontend is only a static preview, and no application code was changed.

**Detected and approved, 2026-08-05:** planned `/compare` is designed as desktop family `567:3592` and mobile family `569:1589`. Coverage includes full 1,440 (`567:3594`), 1,280 first fold (`567:3868`), desktop responsive note (`567:4091`), ten-state desktop contract (`568:106`), full 390 stacked comparison (`569:1591`), 375 first fold (`569:1831`), mobile responsive note (`569:2035`) and ten-state mobile contract (`570:76`). Selection covers empty, one, two and three/max operators; result and recovery cover loading, no comparable result (`568:253`, `570:168`), unknown/restricted market, load error, disclosed outbound confirmation (`568:340`, `570:222`) and redirect failure (`568:369`, `570:242`). Labels `567:3593` and `569:1590` record `APPROVED`; all family/frame node IDs remain unchanged. The family reuses linked approved Header/Footer sources, has no direct-child overflow or fixed-height screen-specific text on audited full/state frames, and has no audited action below 44px. All operator facts and commercial states remain illustrative. No `/compare` route, comparison engine, dedicated comparable projection or trusted market authority is detected; no application code was changed.

**Detected and approved, 2026-08-05:** Programme through Mission 04 is designed as mobile family `580:1713`, with approved status label `580:1714`, responsive contract `580:1715`, journey section `580:1719`, state section `583:1713` and 375 first-fold section `583:2032`. The family contains 17 390×844 journey representatives through Mission 04 and its Dashboard (`580:1721`–`581:1962`), 13 explicit 390×844 identity, claim/session, saving/network, M03 branch, pause and artefact-control states (`583:1715`–`583:2007`), and four 375×667 first folds (`583:2034`, `583:2071`, `583:2098`, `583:2134`). Existing desktop sources `407:699`, `426:2`, `449:1413`, `468:1753` and their Dashboards remain the governing desktop direction. A factual post-M04 correction changes Dashboard `470:358` to `330 XP`, truthful Mission 05 placeholder copy and a disabled next-package action without redesigning the layout. Structural QA reports zero horizontal overflow across all 34 mobile frames, zero content/action collisions on the 30 full frames, auto-height top-level text, protected Help on every frame, and no action below 44px or containing casino/bonus/operator/affiliate promotion. `/program` already implements Missions 01–04, email/password claim/sign-in, Not now, editing and server-owned totals; dedicated visual states for claim/session expiry, duplicate-account recovery, paused M04, destructive artefact deletion and some saving/network recovery remain frontend gaps. No application code was changed; label `580:1714` and family metadata record `APPROVED`, with all family/frame node IDs unchanged.

**Detected and approved, 2026-08-05:** Protected Help is designed as desktop family `599:3886` and mobile family `600:1713`, with approved labels `599:3887` and `600:1714`. Desktop coverage is full 1,440 Hub `599:3891`, full self-exclusion article `599:3972`, resource/handoff state matrix `599:4030` and 1,280 first fold `599:4089`. Mobile coverage is full 390 Hub `600:1718`, full article `600:1792`, outbound confirmation `600:1855`, outbound failure `600:1880`, resource unavailable `600:1913`, governed-content blocked `600:1946`, article unavailable `600:1985` and 375 first fold `600:2010`. Dedicated Header `595:59`, Footer `596:58` and Support Resource Row `597:86` sets keep the route separate from Public Shell commercial navigation. Structural QA reports zero direct-child overflow, zero detached instances, zero audited actions below 44px and no casino/bonus/affiliate terms in action labels. Urgent regional contact copy remains blocked pending approved content. The current frontend still uses commercial `SiteChrome` and legacy Hub/article content; no application code was changed. Labels and family metadata record `APPROVED`; all family/frame/component node IDs are unchanged.

**Detected and approved, 2026-08-05:** Identity & Privacy is designed as desktop family `613:4023` and mobile family `624:1930`. Desktop coverage is returning sign-in `615:4023`, invalid credentials `616:5964`, preserved existing-account claim `616:6020`, public privacy explanation `616:6065`, authenticated data controls `620:2` and 1,280 first fold `620:6182`. Mobile coverage is returning sign-in `624:1933`, invalid credentials `624:1960`, preserved claim `624:1987`, 375 first fold `624:2008`, public privacy explanation `625:1958`, authenticated controls `625:2049` and password-recovery-unavailable `625:2106`. Capability Row set `610:62` supplies linked desktop/mobile available and unavailable variants. Structural QA confirms all 13 frames exist at their documented widths, no screen-specific text layer has zero/tiny height, and 51 component instances remain linked across the family. Better Auth currently supports email/password only; password recovery, account-wide export and account deletion are shown as unavailable rather than actionable. RFC-008 artefact access/edit/erase and sign-out are the only represented current controls. The frontend has no account settings/privacy-control or password-recovery route, and `/privacy` remains placeholder legal content. Labels `613:4024`, `624:1931` and family metadata record `APPROVED`; every node ID is unchanged and no application code was changed.

**Detected and approved, 2026-08-05:** Learning & Articles is designed as desktop family `632:4237` and mobile family `634:2074`. Desktop coverage is full 1,440 catalogue `632:4240`, full 1,440 category `632:4360`, full 1,440 neutral article `633:4341`, evidence/source board `633:4482` and 1,280 article first fold `633:4529`. Mobile coverage is full 390 catalogue `634:2077`, category `634:2177`, neutral article `635:2148`, fail-closed article-unavailable `635:2254`, 375 first fold `635:2304` and evidence/source board `635:2318`. Learning Evidence Source Row set `630:6351` supplies linked desktop/mobile verified, review-due and unavailable variants and remains separate from Programme and Protected Help evidence semantics. Neutral articles place direct educational value, examples, evidence, FAQ and related reading before a disclosed commercial transition; missing required evidence removes the affected claim or holds publication. The approved Public Shell and Footer `Open Help` treatment are reused; the approved protected article nodes `599:3972` and `600:1792` are unchanged. Structural QA reports zero direct-child horizontal overflow, zero tiny screen-specific text layers, zero detached instances and zero audited action frames below 44px. `/learn` catalogue/category/article routes are detected but visually legacy. PR #15 removes the global commercial shell from `/responsible-gambling/**`; the approved dedicated protected shell and governed states remain missing under FE-MIG-13. Search/filter/no-results remains deferred P2. Labels `632:4238`, `634:2075` and both family metadata records record `APPROVED`, with every component, family and frame node ID unchanged.

**Detected and approved, revised 2026-08-05:** Legal / Trust / System is designed as desktop family `646:4467` and mobile family `649:2257`. Desktop coverage is Affiliate Disclosure `646:4469`, Ranking Methodology `646:4558`, About + 18+ `646:4653`, governed legal-content board `648:4623`, system-state board `648:4749`, 1,280 first fold `648:4879` and responsive contract `648:4908`. Mobile coverage is Affiliate Disclosure `649:2259`, Methodology `649:2329`, About + 18+ `649:2405`, governed legal-content board `650:2368`, system-state board `650:2469`, 375 first fold `650:2586` and responsive contract `650:2600`. Legal Document Status `641:6762` supplies six desktop/mobile Published, Review required and Unavailable variants; System Recovery Panel `643:6828` supplies twelve responsive public/protected not-found, load-error and under-review variants. The approved Public Shell is reused unchanged, while Responsible Gambling policy and protected recovery reuse Protected Header `595:59` and Footer `596:58`. Privacy and Terms remain explicit content blocks, generic availability never asserts location, and protected states contain Help/safe return only. Component and screen screenshot QA corrected shared-copy fallback, token fallback, mobile hero wrapping, the Affiliate Disclosure accent collision, section-copy spacing and protected-footer spacing. Current frontend legal routes are legacy or placeholder and system routes are minimal; trusted live jurisdiction authority remains not detected. No application code was changed; labels `646:4468`, `649:2258` and family metadata record `APPROVED`, with all family/frame/component node IDs unchanged.

**Detected, approved and implemented in PR #16, revised 2026-08-05:** Home preserves the full 1,440 desktop source `289:946` and adds desktop family `661:7551` with 1,280 returning-user first fold `661:7554` and contract `661:7607`. Mobile family `657:2545` contains full 390 `657:2548`, approved-shell menu-open first fold `661:2635`, 375 first fold `661:2686` and contract `661:2711`. The mobile page preserves the desktop ten-section narrative through Hero `657:2549`, Programme Theatre `658:2550`, Self Recognition `658:2609`, three full-bleed human chapters `659:2580`, `659:2588`, `659:2596`, Tools `659:2604`, Evidence `659:2658`, final Programme CTA `660:7466` and the linked approved Footer `660:7475`. Frontend PR #16 implements that approved body as server-rendered content with one bounded carousel client island, removes reveal/hydration dependence, and passes responsive checks from 1,440 to 320 CSS pixels. The shared Public Layout remains the server authority for account presentation; authenticated screenshot evidence remains pending because no disposable fixture is detected. The user visually accepted the family on 2026-08-05; labels `661:7552`, `657:2546` and family metadata record `APPROVED`, with all family/frame node IDs unchanged.

**Detected and approved, revised 2026-08-05:** Programme Map / My Plan is designed as desktop family `666:4862` and mobile family `668:2671`. Desktop coverage is full 1,440 after-Mission-04 map `666:4871`, 1,280 after-Mission-01 first fold `666:5014`, Mission 04 paused/re-entry `666:5154` and loading/retry `666:5288`. Mobile coverage is full 390 after-Mission-04 `668:2678`, after-Mission-01 `668:2774`, paused Mission 04 `668:2870`, pause decision `668:2966`, loading/retry `668:2991` and 375 first fold `668:3016`. The family reuses Program Header `388:63`, Path Node `403:62`, Result Artifact `401:105`, Evidence Note `399:52` and approved Dashboard sources; no new library component set was created. It preserves exact server-owned `60 / 140 / 230 / 330 XP` truth, approved Mission 01–04 rewards/order/prerequisites, a disabled Mission 05 next-package state and title/status-only locked Missions 06–10. Screenshot and structural QA corrected the desktop 5 × 2 map, mobile current/resume copy and cloned 375 label size; full frames have no unexpected direct-child overflow, all ten registry titles are exact, and the only overflow is deliberate first-fold cropping. The active frontend renders only a compact three/five-node path and lacks Dashboard-level paused-map and loading/retry representatives. The user visually accepted the family on 2026-08-05; labels `666:4863`, `668:2672` and family metadata record `APPROVED`, with all family/frame node IDs unchanged. No application code was changed.

**Detected and approved, 2026-08-05:** Pause / Support is designed as desktop family `674:5143` and mobile family `674:8171` for `/responsible-gambling/cooling-off`. Desktop coverage is full 1,440 protected article `674:5197`, governed state matrix `674:5230` and 1,280 first fold `674:5250`. Mobile coverage is full 390 article `674:8220`, terms unavailable `674:8279`, local guidance blocked `674:8289`, article unavailable `674:8299` and 375 first fold `674:8314`. Labels `674:5144` and `674:8172` record `APPROVED`; contracts are `674:5145` and `674:8173`. The family reuses approved Protected Header `595:59`, Protected Footer `596:58`, Support Resource Row `597:86` and protected article/recovery sources without changing or detaching them; no new library component set was created. Canonical content is marked `CONTENT REVIEW REQUIRED`, does not assert local tool availability or prescribe duration, and exposes no casino, bonus, offer or affiliate action. Unverified terms and local guidance fail closed to Help Home. Screenshot and structural QA report zero text overlaps, missing fonts, out-of-family children or stale source copy. PR #15 removes the global commercial shell from the active dynamic route without changing its content; the dedicated protected shell and governed fail-closed states remain missing and launch-gated under FE-MIG-13. The user visually accepted the family on 2026-08-05; all family/frame node IDs are unchanged.

**Detected and approved, 2026-08-05:** Commercial Handoff / Affiliate Outbound is designed as desktop family `679:5238` and mobile family `679:8391`. Desktop coverage is disclosure before action `679:5309`, outbound confirmation `679:5319`, action unavailable `679:5329`, redirect failure `679:5339` and 1,280 first fold `679:5349`; mobile coverage is the corresponding four 390 states `679:8476`, `679:8489`, `679:8502`, `679:8515` and 375 first fold `679:8525`. Labels `679:5239` and `679:8392` record `APPROVED`; contracts are `679:5240` and `679:8393`. The family reuses linked approved Public Header instances and approved Casino Profile, Bonuses, Best Offers and Comparison outbound sources; no new library component set was created. Disclosure and material terms precede action, no raw destination is exposed, browser state is not authority, unavailable/failure states do not substitute a partner, and neutral internal recovery remains visible. Structural and screenshot QA report zero overlaps, missing fonts, out-of-family children, raw URLs or stale source copy; actions are 44–54px high. `app/r/[slug]/route.ts` is detected as a deny-safe internal redirect foundation, but canonical eligibility authority and the confirmation/recovery UI contract are not detected. The user visually accepted the family on 2026-08-05; all family/frame node IDs are unchanged and no application code was changed.

**Detected and approved, 2026-08-05:** Age / Market Boundary is designed as desktop family `686:5333` and mobile family `686:8301`. Desktop coverage is age-policy-unresolved `687:5333`, availability-unknown `687:5367`, signal-conflict/stale `687:5402`, restricted/unsupported `688:5398`, policy-unavailable/suspended `688:5433` and 1,280 first fold `688:5468`; mobile coverage is the corresponding five 390 states `687:8398`, `687:8418`, `687:8439`, `688:8488`, `688:8509` and 375 compact first fold `688:8530`. Labels `686:5334` and `686:8302` record `APPROVED`; contracts are `686:5335` and `686:8303`. The family reuses linked approved Public Header instances, mobile/laptop Availability Notice instances and the approved About/18+ and discovery-state grammar without modifying the Public Shell or creating a new library set. It never asserts detected location, verified adulthood, a universal local legal age or live eligibility; commercial actions remain hidden while Programme, Learn and protected Help remain available. Screenshot and structural QA corrected mobile title collisions and clipped 880 action groups; final screen text stays within all twelve frame bounds and action frames are 44px high. A deny-safe shadow resolver is detected, but the default policy store has no approved live dataset and the public frontend does not enforce this contract. The user visually accepted the family on 2026-08-05; all documented family/frame node IDs are unchanged and no application code was changed.

**Detected and approved, 2026-08-05:** Bonus Guide / Explainer is designed as desktop family `694:5455` and mobile family `694:8724`. Desktop coverage is full 1,440 guide `694:5461`, evidence states `694:5531`, 1,280 first fold `694:5542` and article-under-review `694:5551`; mobile coverage is full 390 guide `694:8730`, article-under-review `694:8787`, 375 first fold `694:8800` and evidence states `694:8809`. Labels `694:5456` and `694:8725` record `APPROVED`; contracts are `694:5457` and `694:8726`. The family promotes the approved neutral Learning article sources into a dedicated `/bonus-guide` route contract and reuses linked Public Header/Footer and Learning Evidence Source Row instances without modifying source frames or creating a new library set. Worked values remain illustrative; material restrictions and source state precede comparison; unavailable required evidence removes the claim or holds publication; protected Help stays available through the Footer. Screenshot and structural QA corrected the desktop recovery action alignment and report zero text overflow, missing fonts, unlinked instances or actions below 44px across all eight frames. The active `app/(public)/bonus-guide/page.tsx` remains a legacy card-grid body with a hard-coded `x35` example and no source lifecycle or approved recovery contract; PR #15 changes only its shared shell. The user visually accepted the family on 2026-08-05; all documented family/frame node IDs are unchanged. Editorial/source approval remains a separate release dependency.

The 2026-08-03 RFC-006 amendment makes `Start the 10-Step Program` the only conversion action inside Home content. Commercial discovery remains directly available through the global Header and Footer, while the Home body uses static self-recognition, evidence limitations, a visible Personal Control Plan result, non-testimonial plan-use scenarios and methodology. The 2026-08-05 RFC-007 Help-hierarchy amendment keeps protected Help continuously available in Header/Footer while removing the default standalone public panel. Program, Help, pause, self-check and vulnerability data remain prohibited from advertising targeting and individual commercial personalisation. The RFC-007 article amendment applies the same separation to Learning Center content: neutral commercial education may link contextually to discovery after delivering value, while quit, control, self-exclusion and harm-related articles use a protected-control template with no casino, bonus, offer or affiliate actions in article content.

RFC-002 and its 2026-08-04 active-work amendment remain the approved product authority for the Personal Control Dashboard, mandatory account entry after the first useful Programme mission, 15–25 minute active-work missions, and deterministic Programme rewards. The active Figma file contains the reusable Programme component board (`387:38`), the Mission 01 → Registration → Dashboard → Mission 02 flow (`426:2`), Mission 03 plus its post-mission Dashboard (`449:1413`), and the approved Mission 04 flow plus Dashboard (`468:1753`). RFC-008 governs shared persistence, rewards and privacy; RFC-009 governs Mission 03; RFC-010 governs Mission 04. Mission 01 awards `+60 XP`; Mission 02 awards `+80 XP` and `First Plan`; Mission 03 awards `+90 XP`; Mission 04 awards `+100 XP` and `Boundary built`; same-day completion remains one active day. **Detected:** `/program` implements Missions 01–04, mandatory account claim, returning-user sign-in and their approved Dashboard states. **Detected:** migrations 0011–0017 are applied to the configured remote database. **Historical evidence, 2026-08-04:** the hardened Programme suite had 43 passing tests and the full Node suite had 209 passing tests. **Current audit evidence, 2026-08-05:** typecheck and production build pass, while seven Mission 04 flow tests fail because fixed `reviewAt` fixtures are no longer within the validator's next-30-days window; this is recorded baseline debt, not a product-behaviour change. **Detected limitation:** repository lint remains unconfigured for Next 15 because `npm run lint` invokes interactive/deprecated `next lint`. **Planned release gates:** restore the date-stable Programme regression baseline, authenticated Mission 04 browser completion, clinical-content/compliance review, broader device QA, expiry purge automation, distributed rate limiting, account-wide export/erasure, telemetry and Missions 05–10.

## Technical Baseline Audit

| Field | Value |
| --- | --- |
| Audit date | 2026-07-28 |
| Repository root | `/Users/alex/Documents/Codex/2026-07-09/ns/sevenbet-next` |
| Technical Baseline path | `docs/05_Engineering/Technical_Baseline/` |
| Implementation maturity | Substantial pre-architecture implementation: Next.js public/admin application, Prisma/PostgreSQL, Better Auth, CMS builders, casino/affiliate/media/progress modules, and focused tests are detected. Production-operational and compliance evidence remains incomplete. |
| Architecture readiness score | 5/10 |
| Main blockers before Phase 2 | Authoritative architecture alignment; jurisdiction/licensing and safety-control design; CI/CD, deployment/migration, observability and operational controls; reconciliation of implementation with the approved Product Vision. |

The formal Phase 2 architecture milestone remains pending. A bounded, evidence-backed Programme backend hardening is implemented for Missions 01–04; it does not claim completion of the wider target architecture.

## Pending

⬜ Product Architecture

⬜ Domain Model

⬜ Compliance

🟨 Engineering Standards — Programme-specific architecture, backend and Definition-of-Done standards are implemented; phase-wide standards remain pending

⬜ RFC

## Current Milestone

Complete Product Master Plan while extending the validated Tilt-Locked consumer-product system across public acquisition and active-control surfaces

## Next Milestone

Complete authenticated browser, mobile/device, clinical-content and compliance release review for Mission 04, then design and approve Mission 05 while continuing the remaining operational privacy controls
