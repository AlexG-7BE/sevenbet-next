# Founder Casino Final Block Review

The captures use `/casino/demo-northstar` through the real `data-runtime-renderer="casino-review"` renderer. `HandoffPage` count is zero.

The final demo content now sits in one bounded inner stack: 960px at 1440 and the available 342px content width at 390. The label, casino/score identity, synthetic offer summary and unavailable action all resolve to the final section centre (720px at 1440; 195px at 390).

The final section and the one real `PublicFooter` share a zero-pixel settled boundary. The prior mobile verdict regression remains fixed: `KEEP IN VIEW` and the score composition retain 48px separation. The fixed decision bar, unavailable commercial state, copy and runtime ownership are unchanged.

QA widths: 1440, 1024, 430, 390, 375 and 360. All recorded zero horizontal overflow and no clipping.
