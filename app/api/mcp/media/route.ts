import { retiredMediaResponse } from "@/lib/media-retirement/http";

export const dynamic = "force-dynamic";
export const runtime = "nodejs";

export const POST = retiredMediaResponse;
export const GET = retiredMediaResponse;
export const DELETE = retiredMediaResponse;
export const PATCH = retiredMediaResponse;
export const PUT = retiredMediaResponse;
export const OPTIONS = retiredMediaResponse;
