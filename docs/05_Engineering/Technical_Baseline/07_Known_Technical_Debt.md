# Known Technical Debt

Last repository verification: 2026-08-07 for rows reconciled by DOC-REC-01. Unchanged rows retain the evidence scope of the dated Technical Baseline audit.

Evidence statements are classified as **Detected**, **Inferred**, **Planned** or **Not detected**. This inventory records observed compromises and gaps; it does not prescribe fixes.

| Category | Evidence | Affected area / impact | Severity | Blocks architecture work? |
| --- | --- | --- | --- | --- |
| Documentation governance | **Detected:** DOC-REC-01 assigns current state, roadmap, Figma authority and migration history to separate canonical documents and removes the stale root README roadmap. Historical QA/RFC documents remain intentionally dated evidence. | Current-state claims can drift again if future work duplicates detailed status across canonical and historical documents. | Medium | No; maintain the canonical document roles. |
| Linting gap | `package.json` has `next lint`; no ESLint config/dependency is detected, and Next 15 does not provide that legacy command. | Quality gate is unreliable. | Medium | No. |
| CI/CD gap | No `.github/workflows`, other CI configuration, deployment descriptor, or production migration workflow detected. | Tests/build are not repository-evidenced as automated before release. | High | Yes—target operational architecture must decide this. |
| Legacy admin-preview path | `middleware.ts` and `lib/auth/admin.ts` retain token-based legacy access when `CMS_PHASE1_ALLOW_DEV_ADMIN=true`. | Dual authentication paths increase policy/configuration complexity. | Medium | No, but must be explicitly governed. |
| Compliance/jurisdiction gap | **Detected:** casino country/licence fields, public discovery and a shadow resolver exist. **Not detected:** a trusted live policy dataset or enforced public jurisdiction/licensing authority across all commercial routing. | Product Vision’s regulated-first rule is not technically demonstrated across every commercial decision and handoff. | Critical | Yes. |
| Operational visibility gap | No analytics, monitoring, error-tracking, queue, scheduler, or webhook receiver evidence. | Production incidents, data freshness and integration reliability lack demonstrated controls. | High | Yes. |
| Persistence/deployment state unknown | Migrations exist, but deployed database application state is not in repository evidence. | Cannot establish database readiness/consistency for an environment. | Medium | Yes for deployment architecture. |
| Programme operational controls | RFC-008 uses an in-process limiter and defines 24-hour anonymous expiry, but no shared limiter or automated purge job is detected. | Multi-instance abuse protection and expired private-data cleanup require production operations work. | High | Yes for production scale-out. |
| Programme privacy breadth | Artefact edit/content-erasure is implemented; account-wide export/erasure and verified retention automation are not detected. | The implemented slice is user-controllable, but full account privacy operations remain incomplete. | High | Yes for production account lifecycle. |
| Programme autosave ordering | Mission draft APIs have no revision/client sequence. Task-state completion merges monotonically, but scalar/private fields remain last-write-wins. | A delayed older request can overwrite a newer draft field even though completion/reward state remains protected. | Medium | No; a compatible conflict/resume UX and API decision is required before changing the contract. |
| Programme connected concurrency evidence | Application regression tests exercise concurrent replay and rollback through a deterministic transaction-aware memory unit of work; schema uniqueness and Prisma isolation are source-detected. No multi-instance connected-database contention test is detected. | Production guarantees are strongly constrained but not load-tested across processes against a deployed database. | Medium | No for this bounded refactor; yes before high-scale operational sign-off. |
| CMS coverage ambiguity | Article/legacy CMS models are schema-present but no article admin route was detected. | Scope boundaries and unused/transitioning entities require architecture decisions. | Medium | No. |
| Frontend/design-system consolidation gap | **Detected:** page-level migrations are complete through FE-MIG-16. **Not detected:** consolidated production tokens/components, duplicate CSS resolution, maintained visual regression, Storybook decision, Figma production back-sync, legacy deprecation or Design System v1 governance. | Production patterns can drift or duplicate even though the migrated pages are delivered. | High | Yes; FE-DS-01 is the next frontend architecture workstream. |
| FE-HANDOFF-01 outbound recovery gap | **Detected:** migrated commercial surfaces provide pre-handoff confirmation and `/r/[slug]` fails closed. A denied or failed route still returns a plain no-store text response. **Not detected:** one context-preserving failure/recovery surface with neutral return navigation. | Users can lose route context after a safe denial or resolution failure; route-by-route patches would fragment policy. | High | Yes for the cross-cutting commercial handoff contract. |
| Retained safety-tool product behaviour | `/self-check` uses answer-derived recommendations and can present `/casinos`; the budget tool labels a calculated gambling amount `Recommended` and links to `/bonuses`. | Vulnerability/commercial separation and safe-limit presentation are not production-ready. Both routes remain P0 but are launch-blocked by FE-SAFETY-01. | Critical | Yes. |
| Public accessibility baseline | **Detected:** migrated route packages include focused semantic/keyboard/responsive checks. **Detected debt:** the retained budget tool has label issues. **Not detected:** a maintained automated accessibility runner covering the public surface. | Safety-tool form use and cross-route accessibility regression detection remain incomplete. | High | No, but blocks release acceptance for affected routes and belongs in FE-SAFETY-01/FE-DS-01. |
| Frontend regression tooling | **Detected:** `scripts/visual-qa.mjs` and focused Playwright suites exist. **Not detected:** a maintained approved cross-route visual baseline. | Responsive/Figma drift can recur without review evidence. | Medium | No; FE-DS-01 owns the visual-regression decision. |
| Programme date-dependent tests | **Detected:** fixed historic `reviewAt` values remain in the Mission 04 flow fixtures while validation requires the next 30 days. The 2026-08-05 baseline recorded seven resulting failures. | The suite is not time-stable even though the recorded typecheck and production build passed. | High | No for documentation; yes before Programme release sign-off. |
