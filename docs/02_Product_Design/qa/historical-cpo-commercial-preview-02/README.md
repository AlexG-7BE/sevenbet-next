# HISTORICAL / SUPERSEDED — CPO commercial Preview PR #75

This directory preserves bounded evidence from closed, unmerged
[PR #75](https://github.com/AlexG-7BE/sevenbet-next/pull/75) at exact head
`9156c59a2ce9da783e2b0fcb60698c62602c0b06` and tree
`d9d135d72cdb0992cb7438288b0ded34690aaae7`.

It is **not current product, design, commercial, Programme or Production
authority**. The alternative was never merged. Current public-design authority
is [RFC-034 — Final Design Handoff Public Site](../../../06_RFC/RFC-034-Final-Design-Handoff-Public-Site.md).

## Exact source snapshots

The three files under `source/` are byte-for-byte copies of the material branch
documents at the exact head. Their original locations were:

- `docs/06_RFC/RFC-034-CPO-Commercial-Decision-Layer-Preview-V2.md`;
- `docs/product/CPO-COMMERCIAL-DECISION-LAYER-PREVIEW-V2.md`; and
- `docs/product/CPO-COMMERCIAL-GOLDEN-BEST-CASINOS-V1.md`.

They retain their original historical wording and links. In particular, their
old RFC status must not be interpreted as an active RFC in the current registry.

## Recovery screenshots

The three JPEGs under `screenshots/` were captured read-only on 16 September
2026 between 09:28 and 09:30 UTC from Vercel deployment
`dpl_HVcZ36KABh2zyg2b2oSeM4Zmx1sg`, whose metadata still bound it to the exact
head and branch. They preserve the comparison hub and the two A/B routes as
rendered on that date.

These are **recovery captures, not the original August visual-acceptance
screenshots**. The branch document names ten local `/tmp` screenshot paths, but
those binaries were never Git objects and were not recoverable from the
repository. The live Preview also depends on render-time data/assets, so a
September capture cannot prove the exact August data presentation.

The Preview was `Ready` when inspected on 16 September 2026. Its recorded URLs
were:

- immutable deployment: `https://sevenbet-next-alyiqziq2-alexg-7bes-projects.vercel.app`;
- branch alias: `https://sevenbet-next-git-codex-cpo-commerci-d4239a-alexg-7bes-projects.vercel.app`;
- Variant A: the branch alias plus `/best-casinos`;
- Variant B: the branch alias plus `/best-casinos-roulette`; and
- comparison hub: the branch alias plus `/preview/cpo-commercial-v3`.

`SHA256SUMS` is the integrity authority for the six preserved files.
